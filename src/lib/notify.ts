export const notifyVendor = async (invoiceId: string) => {
  // Placeholder for real notification hook (SendGrid, Twilio, Resend, etc.)
  console.log('Notification placeholder triggered. Vendor notified for payment on Invoice:', invoiceId);
};
